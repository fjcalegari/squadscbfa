<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <import type="android.view.View"/>
        <import type="androidx.lifecycle.LiveData"/>

        <import type="java.util.List" />

        <import type="androidx.paging.PagedList" />

        <import type="com.calestu.squadscbfa.ui.base.Status" />

        <import type="com.calestu.squadscbfa.ui.base.Resource" />

        <import type="com.calestu.squadscbfa.ui.module.player.model.PlayerModelView"/>

        <variable
                name="players"
                type="LiveData&lt;Resource&lt;PagedList&lt;PlayerModelView>>>" />

        <variable
                name="viewmodel"
                type="com.calestu.squadscbfa.ui.module.player.PlayerViewModel" />

        <variable
                name="retryCallback"
                type="com.calestu.squadscbfa.ui.base.RetryCallback" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            >

        <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rvPlayers"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                tools:listitem="@layout/item_player"
                />

        <include
                layout="@layout/loading_state"
                app:resource="@{(Resource) players}"
                app:callback="@{() -> retryCallback.retry()}"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
